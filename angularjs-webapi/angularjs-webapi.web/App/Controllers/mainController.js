"use strict";

app.controller('mainController', [
    '$scope', function($scope) {
        $scope.message = "Welcome to this project, which uses an AngularJS single page application to make calls to a Web API back-end.";
    }
]);
